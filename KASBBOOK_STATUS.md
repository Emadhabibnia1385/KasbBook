# KasbBook v2 — وضعیت و کارهای باقی‌مانده

سندی برای بازبینی بیرونی. هدف این است که یک بازبین بدون دسترسی به مخزن بتواند
بگوید چه چیزی جا مانده، چه چیزی اشتباه طراحی شده، و اولویت بعدی چه باید باشد.

- **مخزن:** `Emadhabibnia1385/KasbBook` — برنچ `v2`
- **کامیت:** `6514b3e`
- **تاریخ این سند:** ۱۴۰۵/۰۶/۰۳ (2026-08-25)

---

## ۱. محصول در یک پاراگراف

ربات حساب‌داری کسب‌وکار کوچک، self-hosted. کاربر از تلگرام (و بله و روبیکا)
تراکنش ثبت می‌کند — یا با دکمه یا با یک خط متن مثل `فروش ۲۵۰ک`. یک HTTP API هم
دارد که همان سرویس‌ها را بیرون می‌دهد. پول دقیق (Decimal)، تقویم جلالی، و پشت هر
تراکنش یک سند دوطرفهٔ تراز.

## ۲. پشتهٔ فنی

Python 3.12 · FastAPI · SQLAlchemy 2 (async) · PostgreSQL 16 · Alembic · Redis ·
Argon2id · PyJWT · httpx · systemd · Docker Compose

## ۳. اعداد

| | |
|---|---|
| کد (`src/` + `apps/`) | ۱۱٬۲۲۳ خط |
| تست (`tests/v2/`) | ۶٬۶۰۴ خط |
| تست‌ها | ۴۰۶ سبز روی ۳٫۹ · ۴۰۸ روی ۳٫۱۲ (۲ تای packaging به `tomllib` نیاز دارند) |
| مسیرهای API | ۶۶ |
| مایگریشن | ۵ |
| جدول | ۲۹ |
| CI | ۲ job، هر دو سبز |
| تولید | فعال روی سرور، صفر ری‌استارت |

---

## ۴. معماری

```
  تلگرام / بله / روبیکا
          │
    adapters/            فقط ترجمهٔ payload — نه دیتابیس، نه قاعده
          │
    bot/                 صفحه‌ها و مسیریابی، مستقل از پیام‌رسان
          │
  api/ ───┤              parse، صدا زدن سرویس، ترجمهٔ خطا
          │
    modules/*/service.py  هر قاعده، دقیقاً یک بار
          │
    modules/*/models.py + shared/
          │
   PostgreSQL · Redis · Alembic
```

**قواعدی که با تست اجبار شده‌اند (نه صرفاً توافق):**

- هیچ آداپتوری SQLAlchemy یا سرویس import نمی‌کند — تستی گراف import را می‌پیماید
- هیچ پیشوند callback دوبار مسیریابی نمی‌شود، و هیچ پیشوندی که صفحه‌ای تولید
  می‌کند بی‌مقصد نمی‌ماند
- هر متد خروجیِ آداپتور یک مورد تست دارد
- مایگریشن‌ها هیچ کد اپلیکیشن import نمی‌کنند
- مدل و مایگریشن drift ندارند (با `compare_type` واقعی، نه خاموش)
- `pyproject.toml` و `requirements-v2.txt` هم‌خوان می‌مانند
- `runner.py` وقتی مثل systemd به‌عنوان اسکریپت اجرا شود کار می‌کند

---

## ۵. آنچه انجام شده

### هسته
- [x] هویت: یک حساب، چند identity — هیچ آی‌دی پیام‌رسانی خودِ حساب نیست
- [x] لینک دوطرفه با توکن یک‌بارمصرف (فقط digest ذخیره می‌شود، TTL ۱۵ دقیقه)
- [x] دفترها: شخصی / کسب‌وکار / تیمی / سازمانی
- [x] نقش و دسترسی؛ تنها دروازه `books.require(...)`
- [x] دفتر کل دوطرفه، `trial_balance` در تست‌ها assert می‌شود
- [x] چندارزی — نرخ در لحظهٔ تراکنش snapshot می‌شود و دوباره اعمال نمی‌شود
- [x] تقویم جلالی؛ تبدیل به بازهٔ میلادی پیش از هر کوئری

### فیچرها
- [x] ثبت سریع تک‌خطی + پیشنهاد دسته‌های اخیر
- [x] گزارش (ماه/سال/هفته/دسته) + خروجی CSV
- [x] بودجه، طلب و بدهی، وام و اقساط، قواعد تکرارشونده
- [x] رسید: با file_id پیام‌رسان، بدون دانلود و ذخیره
- [x] یادآور و خلاصهٔ روزانه، به وقت خودِ کاربر
- [x] حقوق: دوره، سهم اعضا، کارکرد، کسر و اضافه، فیش، پرداخت مرحله‌ای
- [x] خزانه: صندوق‌ها و قواعدشان، پیش از تقسیم سود

### پیام‌رسان‌ها
- [x] تلگرام (Bot API رسمی)
- [x] بله (Bot API رسمی — همان dialect تلگرام)
- [x] روبیکا (Bot API رسمی v3 — پروتکل متفاوت)
- [x] مجموعهٔ سازگاری: همان assertها روی هر سه

### API
- [x] ۶۶ مسیر: auth، دفتر، تراکنش، گزارش، بودجه/طلب/وام، حقوق/خزانه، وبهوک، سلامت
- [x] JWT کوتاه‌عمر + refresh چرخشی با تشخیص سرقت (باطل‌کردن کل خانواده)
- [x] کلید API برای برنامه‌ها
- [x] محدودیت نرخ per-IP و per-account
- [x] پول به‌صورت **رشته** از مرز رد می‌شود

### عملیات
- [x] `install.sh` / `update.sh` (با عقب‌گرد خودکار) / `backup.sh` / `restore.sh` / `uninstall.sh`
- [x] یونیت‌های systemd داخل مخزن، با hardening
- [x] Docker Compose برای Postgres و Redis (جدا از instance مشترک سرور)
- [x] CI: lint + کل تست‌ها + **تمرین مایگریشن روی Postgres واقعی با جدول‌های پر**
- [x] ۱۳ صفحه مستندات + README دوزبانه

---

## ۶. کارهای باقی‌مانده

### الف) بلوکه — منتظر تصمیم مالک محصول

- [ ] **سیستم ساخت حساب کاربری** — عمداً دست‌نخورده مانده تا مالک محصول
      مشخص کند چه می‌خواهد

### ب) به حساب کاربری چسبیده

- [ ] **حذف حساب کاربری**
      اکنون هیچ مسیری وجود ندارد، و کلیدهای خارجی هم جلویش را می‌گیرند:
      کاربر مالک دفتر است، دفتر مالک حساب‌هاست، و حساب‌ها از `journal_lines`
      ارجاع می‌شوند.
      این پیش‌فرض برای یک دفتر کل **درست** است — نباید بشود با یک `DELETE`
      دفتری را غیرقابل‌اثبات کرد — ولی «هیچ راهی نیست» هم جواب نیست.
      طرح پیشنهادی: انتقال مالکیت → ناشناس‌کردن ردیف کاربر → نگه‌داشتن سند.
      **سؤال برای بازبین:** آیا این طرح با الزامات نگه‌داری سوابق مالی و با
      حق حذف داده سازگار است؟

- [ ] **ورود دومرحله‌ای (TOTP)**
      ماشین‌آلات توکن آماده است. نیاز: یک جدول، یک مرحله در لاگین، کدهای بازیابی.

- [ ] **بازیابی رمز**
      ساخته نشده چون مسیر ایمیل وجود ندارد. بدون SMTP این فقط یک تیکت پشتیبانی
      است که ادای فیچر درمی‌آورد.

### ج) عمداً به تعویق افتاده (خواستهٔ مالک محصول)

- [ ] **پنل وب** — API‌اش کامل است؛ این یک پروژهٔ front-end است نه back-end
- [ ] **ایتا** — `Provider.EITAA` در انوم هست و درخواستش با پیام «no adapter»
      در startup رد می‌شود، نه با `AttributeError` سه صفحه بعد

### د) کوچک

- [ ] **رسید غیرعکسی** — سند و ویس پارس می‌شوند، ولی فقط عکس به رسید وصل است
- [ ] **دورهٔ بودجه** — پنجره در `BudgetService.status` روی ماه جلالی hardcode
      است؛ اصلاً ستون دوره وجود ندارد
- [ ] **خروجی غیر CSV** — حسابدار واقعی فرمتی می‌خواهد که نرم‌افزارش import کند
- [ ] **وبهوک در تولید** — مسیر هست و تست دارد؛ polling کار می‌کند. سوئیچ‌کردن
      تصمیم استقرار است نه کد. (polling از پشت هر NAT بدون پورت ورودی کار می‌کند)

---

## ۷. شکاف‌ها و ریسک‌های شناخته‌شده

این‌ها صریح نوشته می‌شوند، نه پنهان:

1. **بله و روبیکا هرگز با API زندهٔ خودشان روبه‌رو نشده‌اند.**
   بر اساس مستندات رسمی نوشته شده‌اند و همان مجموعهٔ سازگاری تلگرام را پاس
   می‌کنند — ولی روی mock transport. اولین برخورد با توکن واقعی ممکن است
   تنظیم بخواهد.

2. **بله و روبیکا هیچ امضایی روی وبهوک ندارند.**
   `verify_webhook` صادقانه `True` برمی‌گرداند و docstring می‌گوید نمی‌تواند
   ثابت کند تماس‌گیرنده کیست. محافظت از مسیرِ حاویِ راز است، با مقایسهٔ
   constant-time. این از امضا ضعیف‌تر است — و همان چیزی است که خودِ providerها
   ممکن می‌کنند.
   **سؤال برای بازبین:** جایگزین بهتری هست؟

3. **محدودیت نرخ بدون Redis درون‌پروسه است** — روی یک worker درست، روی چند
   worker بی‌فایده. API موقع startup هشدار می‌دهد.

4. **حلقهٔ یادآور پیش از تأیید ثبت می‌کند** — عمدی، چون پیام تکراری بدتر از
   نرسیدن است، ولی یعنی یک شکست در ارسال، خلاصهٔ آن روز را می‌سوزاند.

5. **catch-up قواعد تکرارشونده روی ۴۰۰ بار سقف دارد** — قاعده‌ای که سال‌ها عقب
   افتاده در یک اجرا کامل جبران نمی‌شود.

6. **تست‌های packaging زیر ۳٫۱۱ skip می‌شوند** (`tomllib`). CI روی ۳٫۱۲ است.

---

## ۸. باگ‌هایی که در همین دوره پیدا و بسته شدند

برای اینکه بازبین بداند چه چیزی قبلاً بررسی شده:

| باگ | چرا مهم بود |
|---|---|
| دو پکیج هم‌نام `kasbbook` | سه بار `ModuleNotFoundError: telegram` داد — ماژولی که اصلاً درخواست نشده بود. نسل اول از برنچ حذف شد. |
| `ADD COLUMN NOT NULL` روی جدول پر | سه دیپلوی «موفق» گزارش شد چون خروجی alembic از `grep` رد می‌شد. حالا تمرین روی Postgres واقعی در CI. |
| `send_plain` و `send_stored_file` | هر دو `TypeError` می‌دادند. خلاصهٔ روزانه و نمایش رسید کار نمی‌کردند. هیچ‌کدام پوشش نداشتند. |
| تشخیص سرقت توکن | خانواده را باطل می‌کرد، بعد خطا پرتاب می‌کرد، و همان خطا rollback می‌آورد که باطل‌سازی را برمی‌گرداند. |
| `Provider.TELEGRAM` هاردکد در رانر | آپدیت بله به حساب تلگرامیِ هم‌شماره وصل می‌شد. |
| autogenerate و `Money` | ۳۰ `ALTER COLUMN TYPE` بی‌معنی روی Postgres تولید می‌کرد. |
| `ShareRule` فقط خوانده می‌شد | حقوق روی دفتر واقعی صفر فیش می‌داد. تست‌ها نگرفتند چون setup مستقیم در session می‌نوشت. |
| `update.sh` فایل خودش را وسط اجرا بازنویسی می‌کرد | بش با آفست بایتی می‌خواند؛ ترکیبی از دو نسخه اجرا شد. یک مرحله جا افتاد و health گفت «سالم» در حالی که API پایین بود. |

---

## ۹. سؤال‌های مشخص برای بازبین

1. طرح **حذف حساب** (انتقال مالکیت → ناشناس‌سازی → نگه‌داشتن سند) با الزامات
   حسابداری و حق حذف داده سازگار است؟
2. برای وبهوک providerهایی که امضا ندارند، آیا راهی بهتر از «مسیر حاوی راز»
   وجود دارد؟
3. اولویت‌بندی بخش ۶ درست است؟ چیزی هست که باید جلوتر باشد؟
4. در بخش ۷ ریسکی هست که دست‌کم گرفته شده؟
5. با توجه به الگوهای باگ بخش ۸، چه دسته‌ای از خطا احتمالاً هنوز باز است؟
