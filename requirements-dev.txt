# Development / CI only. The bot itself needs nothing from this file.
-r requirements.txt

pyflakes==3.2.0
pytest>=8.3
pytest-asyncio>=0.24

# Alembic's command API is synchronous; the async driver cannot serve it.
psycopg[binary]>=3.2

# The documentation site. Pinned below 2.0: the Material maintainers have said
# MkDocs 2.0 removes the plugin system with no migration path, so an unpinned
# major would break the build on somebody else's schedule.
mkdocs-material>=9.5,<10
mkdocs-static-i18n>=1.2
