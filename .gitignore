# --- secrets ---------------------------------------------------------------
.env
*.env
!.env.example

# --- runtime data (never commit a customer's books) -------------------------
KasbBook.db
KasbBook.db-wal
KasbBook.db-shm
*.db
*.db-wal
*.db-shm
backups/

# --- python ----------------------------------------------------------------
venv/
.venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- editors / OS ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# --- alembic scratch ---
.migration-check.db

# --- deploy secrets ---
deploy/.env

# A status snapshot written for an outside review. It duplicates
# docs/roadmap.md and goes stale within a day, so it stays out of the repo.
KASBBOOK_STATUS.md
