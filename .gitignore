# --- secrets ---------------------------------------------------------------
.env
*.env
!.env.example

# --- runtime data (never commit a customer's books) -------------------------
KasbBook.db
KasbBook.db-wal
KasbBook.db-shm
*.db
*.db-wal
*.db-shm
backups/

# --- python ----------------------------------------------------------------
venv/
.venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- editors / OS ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# --- graphify knowledge graph (generated) -----------------------------------
# Rebuilt from source by `graphify update .`, so it is derived data, not source.
# The official install only asks for .claude/ and CLAUDE.md to be versioned.
graphify-out/
graph.json
.graphify_analysis.json

# --- alembic scratch ---
.migration-check.db

# --- deploy secrets ---
deploy/.env
