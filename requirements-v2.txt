# Runtime for the v2 core. Pinned loosely for now; a lock file lands with the
# Docker image, where the exact versions actually matter.
fastapi>=0.115
sqlalchemy[asyncio]>=2.0
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.5
asyncpg>=0.30
aiosqlite>=0.20
argon2-cffi>=23.1
pyjwt>=2.9
httpx>=0.27
jdatetime>=5.0
python-multipart>=0.0.9
redis>=5.0
